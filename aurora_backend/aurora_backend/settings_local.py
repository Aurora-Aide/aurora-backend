"""
Local/development settings.
"""

from .settings_base import *

DEBUG = True
ALLOWED_HOSTS = ["*"]

# Example local overrides (leave commented for now):
# EMAIL_BACKEND = "django.core.mail.backends.console.EmailBackend"

